import React from 'react';
import Dashboard from '@/features/dashboard/Dashboard';

/**
 * TabIndex Component
 * Passes through to the segmented Dashboard feature.
 * @returns {React.ReactElement}
 */
export default function TabIndex() {
  return <Dashboard />;
}